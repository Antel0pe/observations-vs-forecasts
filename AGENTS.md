# Agent Instructions

- Always use the conda environment named `observations-vs-forecasts` when working in this repository.
- Always prefer using conda to install packages over pip if it exists.
- Activate it before running project commands: `conda activate observations-vs-forecasts`.
- In non-interactive shells, prefer: `conda run -n observations-vs-forecasts <command>`.
